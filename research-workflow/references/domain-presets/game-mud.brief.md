# game-mud 预设 brief 增量

> 本文件由 `create_research_skeleton.py --preset game-mud` 读取并注入到 `00-brief/brief.md` 的目标与范围章节。给人读的说明见 `game-mud.md`。

## 目标补充

1. **忠实还原原始细节**：基于当前仓库一手源码，细致梳理该子系统的实现方式、数据结构、调用链与状态流转。
2. **提取设计灵感与风险警示**：从现代游戏设计、玩家心理、商业化与 UGC 创作角度，输出对新引擎可参考的方向、应避免的过时模式以及需警惕的设计陷阱。
3. **不输出 engine 接口草案**：本次调研止步于设计输入层，具体的 engine 抽象与接口设计留待后续任务单独决策。

## 范围补充

### 不纳入范围

- 不做行为等价验证。
- 不把 engine 侧现有实现当作正确形态反向脑补。
- 不依赖旧文档结论。
- 不输出可直接落地的 engine 代码或接口契约。

## 现代视角范围

至少包含：现代玩法设计、玩家心理与留存、商业化与增长三个视角，按 grilling 确认的权重取舍。
