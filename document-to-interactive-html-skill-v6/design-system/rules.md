# Design Rules

Use:

One primary color.
One accent color.
Neutral grayscale.

Typography hierarchy:
Title > Section > Body > Detail

Density:
One screen = one key idea.

Avoid:
random colors,
excessive shadows,
too many cards,
meaningless animation.
